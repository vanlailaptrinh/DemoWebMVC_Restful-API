<!-- Là phần view đơn thuần chưa có xử lí gì -->
    @{ViewData["Title"] = "Giỏ hàng";
}

<div class="container mt-4">
    <h2 class="text-center">@ViewData["Title"]</h2>

    <div class="row">
        <div class="col-md-12">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Sản phẩm</th>
                        <th>Số lượng</th>
                        <th>Giá</th>
                        <th>Trạng thái</th>
                    </tr>
                </thead>
                <tbody>

                      @* @if (Model != null && Model.Any())
                      {
                          foreach (var order in Model)
                          {
                              <tr>
                                  <td>@order.ProductName</td>
                                  <td>
                                      <input type="number" class="form-control" value="@order.Quantity" />
                                  </td>
                                  <td>@order.Price</td>
                                  <td>
                                      <button class="btn btn-sm btn-warning">Cập nhật</button>
                                      <button class="btn btn-sm btn-danger">Xóa</button>
                                  </td>
                              </tr>
                          }
                      }
                      else
                      {
                          <tr>
                              <td colspan="4" class="text-center">Không có đơn hàng nào trong giỏ</td>
                          </tr>
                      } *@
                </tbody>
            </table>
        </div>
    </div>

    <!-- Nút Thanh toán -->
    <footer class="mt-4">
        <div class="text-end">
            <button class="btn btn-success btn-lg">Thanh toán</button>
        </div>
    </footer>
</div>
