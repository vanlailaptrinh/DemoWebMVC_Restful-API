<!-- Là phần view đơn thuần chưa có xử lí gì -->
@{
    ViewData["Title"] = "Đơn Hàng";
}

<div class="container mt-4">
    <h2 class="text-center">@ViewData["Title"]</h2>

    <div class="row">
        <div class="col-md-12">
            <table class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>Mã Đơn Hàng</th>
                        <th>Tên Khách Hàng</th>
                        <th>Ngày Thanh Toán</th>
                        <th>Tổng Tiền</th>
                        <th>Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @*  @if (Model != null && Model.Any())
                    {
                        foreach (var order in Model)
                        {
                            <tr>
                                <td>@order.OrderId</td>
                                <td>@order.CustomerName</td>
                                <td>@order.PaymentDate.ToString("dd/MM/yyyy")</td>
                                <td>@order.TotalAmount.ToString("C")</td>
                                <td>
                                    <a asp-controller="HangHoa" asp-action="Edit" asp-route-id="@order.OrderId" class="btn btn-primary">Sửa</a>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center">Chưa có đơn hàng nào</td>
                        </tr>
                    }  *@
                </tbody>
            </table>
        </div>
    </div>
</div>
